<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <!-- Hunt AI feedback + calibration store (HUNT-AI-BACKEND-SCOPE §1d).
         Records analyst 👍/👎 (+ optional correction) on AI verdicts/leads; the source
         data the verdict endpoint's AiCalibration block is computed from. -->
    <changeSet id="20260904001-1" author="hivearmor">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="ha_ai_feedback"/>
            </not>
        </preConditions>

        <createTable tableName="ha_ai_feedback">
            <column name="id" type="bigserial" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="tenant" type="varchar(200)" defaultValue="default">
                <constraints nullable="true"/>
            </column>
            <!-- 'verdict' | 'lead' -->
            <column name="target_type" type="varchar(16)">
                <constraints nullable="false"/>
            </column>
            <column name="target_id" type="varchar(128)">
                <constraints nullable="false"/>
            </column>
            <!-- calibration bucket, e.g. "credential-access verdicts" -->
            <column name="verdict_scope" type="varchar(128)" defaultValue="unscoped">
                <constraints nullable="true"/>
            </column>
            <!-- 'up' | 'down' -->
            <column name="vote" type="varchar(8)">
                <constraints nullable="false"/>
            </column>
            <!-- optional analyst correction: malicious|suspicious|benign|inconclusive -->
            <column name="corrected_verdict" type="varchar(16)">
                <constraints nullable="true"/>
            </column>
            <column name="note" type="text">
                <constraints nullable="true"/>
            </column>
            <column name="user_login" type="varchar(200)">
                <constraints nullable="true"/>
            </column>
            <column name="created_at" type="timestamp" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- calibration is computed by (tenant, verdict_scope) windowed on created_at -->
        <createIndex indexName="idx_ha_ai_feedback_scope" tableName="ha_ai_feedback">
            <column name="tenant"/>
            <column name="verdict_scope"/>
            <column name="created_at"/>
        </createIndex>

        <createIndex indexName="idx_ha_ai_feedback_target" tableName="ha_ai_feedback">
            <column name="target_type"/>
            <column name="target_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
