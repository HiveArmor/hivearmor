<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <!--
      SPEC-04 (W1b) PR-1b.2c-ii backfill + NOT-NULL for hive_collectors.tenant_id
      (added by 20260913007).

      Same contract as 20260910003 / 20260913002 / 20260913004 / 20260913006:
        * SINGLE-TENANT (no MSSP-managed client with a prefix): every legacy row's
          tenant is 0 — backfill to 0 is correct and COMPLETE, NOT-NULL enforced.
          Gated by the ha_client sqlCheck.
        * MSSP: MARK_RAN (no-op). Going forward, each collector's tenant is set from
          the agent-manager Collector proto (field 9) at sync; legacy MSSP rows are
          re-stamped on the next sync from the manager (the tenant authority).
          NOT-NULL for MSSP deferred.
    -->
    <changeSet id="20260913008-1" author="hivearmor">
        <preConditions onFail="MARK_RAN">
            <and>
                <columnExists tableName="hive_collectors" columnName="tenant_id"/>
                <sqlCheck expectedResult="0">
                    SELECT COUNT(*) FROM ha_client WHERE mssp_managed = TRUE AND client_prefix IS NOT NULL
                </sqlCheck>
            </and>
        </preConditions>
        <update tableName="hive_collectors">
            <column name="tenant_id" valueNumeric="0"/>
            <where>tenant_id IS NULL</where>
        </update>
        <addNotNullConstraint tableName="hive_collectors" columnName="tenant_id"
                              columnDataType="BIGINT" defaultNullValue="0"/>
    </changeSet>

</databaseChangeLog>
