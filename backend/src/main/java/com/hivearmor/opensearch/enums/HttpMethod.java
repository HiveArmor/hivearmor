package com.hivearmor.opensearch.enums;

/**
 * HTTP method enum for raw OpenSearch HTTP requests (ISM policy management).
 * Replaces com.hivearmor.opensearch_connector.enums.HttpMethod
 */
public enum HttpMethod {
    GET, POST, PUT, DELETE, HEAD, PATCH
}
