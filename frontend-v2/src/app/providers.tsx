"use client";

// NOTE: After running `npm install`, @tanstack/react-query will be available.
// Until then this is a passthrough wrapper.
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
