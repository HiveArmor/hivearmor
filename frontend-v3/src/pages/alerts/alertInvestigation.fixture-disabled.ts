import type { AlertInvestigation } from './alertInvestigation.types';

/** Unreachable in production because fixture mode is DEV-gated. */
export const foundationAlertInvestigation = undefined as unknown as AlertInvestigation;
