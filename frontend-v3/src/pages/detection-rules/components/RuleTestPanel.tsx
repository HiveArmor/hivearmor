/**
 * RuleTestPanel — Placeholder for future rule test panel (S23)
 * Will be implemented in DEF-03 session
 */

export function RuleTestPanel(): JSX.Element {
  return (
    <div style={{ padding: 24 }}>
      <div style={{ color: 'var(--ha-text-secondary)', fontSize: 'var(--ha-text-sm)' }}>
        Test panel will be implemented in DEF-03.
      </div>
    </div>
  );
}
