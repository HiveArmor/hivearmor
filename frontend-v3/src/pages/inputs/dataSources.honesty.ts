/** Bundle-visible job sentence — source inventory ops, not integrations or connector SDK staging. */
export const DATA_SOURCES_JOB_SENTENCE =
  'Data sources operations — inventory configured ingestion adapters, adapter reachability and last-event signals for Analysts and Platform Administrators. Pipeline flow and soak metrics live on Pipeline Signals; parser authoring on Data Parsing; legacy connectors on Integrations — source onboarding, secret lifecycle and governed replay remain fail-closed until ING contracts land.';

export const DATA_SOURCES_ONBOARD_FAIL_CLOSED_TITLE =
  'Source onboarding remains unavailable until ING-002 publishes durable draft/activate lifecycle with validation receipts.';
