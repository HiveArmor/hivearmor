/**
 * GeneralTab — placeholder scaffold.
 *
 * Full form UI wiring (site name, timezone, locale) will be implemented
 * in a follow-up task. This scaffold renders a stable mount point so
 * the SystemSettingsPage tab layout compiles and routes correctly.
 *
 * Requirements: 1.2, 13.5, 13.9, 13.10
 */

export function GeneralTab(): JSX.Element {
  return (
    <div
      style={{
        padding: 'var(--pf-v5-global--spacer--lg, 1.5rem)',
        color: 'var(--ha-text-secondary)',
      }}
    >
      {/* General settings form — implementation pending */}
    </div>
  );
}
