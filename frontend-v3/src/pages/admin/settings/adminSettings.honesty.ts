/** Bundle-visible job sentence — secret-safe platform configuration, not audit ledger or integrations ops. */
export const ADMIN_SETTINGS_JOB_SENTENCE =
  'Platform Settings — review secret-safe effective configuration for Platform Administrators. Masked values load from GET /api/ha-admin/settings; immutable audit evidence on Audit; retention policy on Retention — versioned change control, approval, rollout receipts and legacy /ha-settings migration remain fail-closed until GOV-007–GOV-010 land.';

export const ADMIN_SETTINGS_PROPOSE_FAIL_CLOSED_TITLE =
  'Configuration propose/apply remains unavailable until GOV-007 publishes versioned drafts, impact preview, approval and rollout receipts.';

/** Governed configuration change contracts — not PRODUCTION READY from UI alone. */
export const GOV_SETTINGS_CHANGE_CONTROL_LIVE = false;

/** Legacy /ha-settings successor migration — not advertised as deprecated until headers land (GOV-008). */
export const GOV_LEGACY_SETTINGS_MIGRATION_COMPLETE = false;
