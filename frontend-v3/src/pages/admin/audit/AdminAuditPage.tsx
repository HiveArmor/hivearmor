/**
 * AdminAuditPage — Placeholder (S06)
 */

export function AdminAuditPage(): JSX.Element {
  return (
    <div style={{ padding: 24, color: 'var(--ha-text-primary)' }}>
      <h1 style={{ fontSize: 'var(--ha-text-2xl)', marginBottom: 16 }}>Audit Log</h1>
      <p style={{ color: 'var(--ha-text-secondary)' }}>Placeholder page — implementation in later session.</p>
    </div>
  );
}
