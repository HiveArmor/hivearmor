/** Bundle-visible job sentence — data lifecycle governance, not audit ledger or platform settings. */
export const ADMIN_RETENTION_JOB_SENTENCE =
  'Data retention governance — review searchable retention periods, archive targets and lifecycle posture from GET /api/ha-retention-policies for Platform Administrators. Immutable audit evidence on Audit; secret-safe settings on Platform Settings — legal holds, impact preview, versioned change proposals and archive orchestration remain fail-closed until GOV-005 lands.';

/** GOV-005 — versioned retention revision drafts and approval are not exposed. */
export const ADMIN_RETENTION_PROPOSE_FAIL_CLOSED_TITLE =
  'Retention revision proposals remain fail-closed until GOV-005 publishes versioned draft, impact preview and approval contracts.';

/** Governed archive orchestration and legal-hold registry — not PRODUCTION READY from UI alone. */
export const GOV_RETENTION_LIFECYCLE_LIVE = false;
