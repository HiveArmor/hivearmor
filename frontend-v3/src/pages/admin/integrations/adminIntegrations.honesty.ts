/** Bundle-visible job sentence — connector/delivery/access ops, not identity or pipeline authoring. */
export const INTEGRATIONS_JOB_SENTENCE =
  'Integrations operations — inventory configured connectors, delivery receipts and service-access aliases for Platform Administrators. Notification routing lives on Notifications; API credentials on API Keys; ingestion sources on Pipeline Signals — this workspace does not claim vendor-live validation or governed connector activation until INO contracts land.';

export const INTEGRATIONS_CONFIGURE_FAIL_CLOSED_TITLE =
  'Connector and destination setup remain unavailable until INO-001–INO-004 publish versioned validate/activate contracts.';
