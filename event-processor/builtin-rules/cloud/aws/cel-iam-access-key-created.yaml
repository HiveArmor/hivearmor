id: 5212
name: CEL-AWS-IAM-ACCESS-KEY
dataTypes:
  - aws
  - cloudtrail
category: Persistence
technique: "T1098.001 - Account Manipulation: Additional Cloud Credentials"
adversary: origin
description: IAM access key created (CreateAccessKey).
references:
  - https://attack.mitre.org/techniques/T1098/001/
deduplicateBy:
  - origin.user
  - origin.ip
impact:
  confidentiality: 3
  integrity: 2
  availability: 1
where: >
  (has(log.eventName) && string(log.eventName) == "CreateAccessKey")
  || raw.matches("(?i).*(eventName[\"'=: ]+CreateAccessKey|CreateAccessKey).*")
