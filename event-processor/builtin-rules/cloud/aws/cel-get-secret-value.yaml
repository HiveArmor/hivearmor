id: 5293
name: CEL-AWS-GET-SECRET
dataTypes:
  - aws
  - cloudtrail
category: Credential Access
technique: "T1555 - Credentials from Password Stores"
adversary: origin
description: Secrets Manager GetSecretValue.
references:
  - https://attack.mitre.org/techniques/T1555/
deduplicateBy:
  - origin.ip
  - origin.user
impact:
  confidentiality: 3
  integrity: 2
  availability: 1
where: >
  (has(log.eventName) && string(log.eventName) == "GetSecretValue")
  || raw.matches("(?i).*eventName[\"\'=: ]+GetSecretValue.*")
