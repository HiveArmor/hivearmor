id: 5286
name: CEL-NET-IRC
dataTypes:
  - dns
  - netconn
  - netflow
category: Command and Control
technique: "T1071 - Application Layer Protocol"
adversary: origin
description: IRC protocol / classic botnet C2 ports.
references:
  - https://attack.mitre.org/techniques/T1071/
deduplicateBy:
  - origin.ip
  - destination.ip
impact:
  confidentiality: 3
  integrity: 2
  availability: 1
where: 'raw.matches("(?i).*(dst.?port|destination.?port|dport)[^0-9]{0,12}(6667|6697|7000).*") || raw.matches("(?i).*(PRIVMSG|JOIN #|NICK ).*")'
